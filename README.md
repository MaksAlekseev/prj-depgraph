# prj-depgraph

Проект по конфигурационному управлению.

## Как запустить
1. Скопируй config.ini.example в config.ini.
2. Запусти программу:
   ```bash
   python src/stage1_minimal.py --config config.ini
